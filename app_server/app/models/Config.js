/*
 * Edit to your local mysql database's credentials
 */

var config = {};

config.name = '';
config.username = '';
config.password = '';
config.host = 'localhost';
config.dialect = 'mysql';

module.exports = config;
